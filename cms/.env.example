# Copy to cms/.env.local. Both values are public — they ship inside the
# JavaScript every visitor downloads. They are safe there because row-level
# security denies anon everything.
#
# Take them from the repo root .env (SUPABASE_URL and whichever of
# SUPABASE_PUBLISHABLE_KEY / SUPABASE_ANON_KEY your project accepts).

VITE_SUPABASE_URL=https://krwisemsvmwpvunnsnwp.supabase.co
VITE_SUPABASE_ANON_KEY=
