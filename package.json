{
  "name": "televizio",
  "private": true,
  "type": "module",
  "workspaces": [
    "cms"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "db:test": "node --env-file=.env supabase/tests/run.mjs",
    "db:push": "supabase db push",
    "test:api": "node --env-file=.env ./node_modules/vitest/vitest.mjs run tests/functions",
    "fn:deploy": "supabase functions deploy publish --use-api && supabase functions deploy create-admin --use-api",
    "vercel-build": "node scripts/build-site.mjs"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "jsdom": "^25.0.0",
    "pg": "^8.23.0",
    "supabase": "^2.116.0",
    "typescript": "^5.6.0",
    "vercel": "^59.11.0",
    "vitest": "^2.1.0"
  }
}
